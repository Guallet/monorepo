{
  "name": "@guallet/auth",
  "version": "0.1.0",
  "description": "Guallet Auth package for managing authentication with Better Auth",
  "private": true,
  "license": "Apache-2.0",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "lint": "oxlint .",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "better-auth": "workspace:*",
    "react": "workspace:*"
  },
  "devDependencies": {
    "tsconfig": "workspace:*",
    "typescript": "workspace:*"
  }
}
