export type BorderRadius = {
  xs: number;
  sm: number;
  md: number;
  lg: number;
  xl: number;
};

export const defaultBorderRadius: BorderRadius = {
  xs: 2,
  sm: 4,
  md: 8,
  lg: 16,
  xl: 32,
};
