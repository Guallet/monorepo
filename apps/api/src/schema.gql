# ------------------------------------------------------
# THIS FILE WAS AUTOMATICALLY GENERATED (DO NOT MODIFY)
# ------------------------------------------------------

type Institution {
  id: ID!
  name: String!
  image_src: String
  user_id: String
}

type Account {
  id: ID!
  name: String!
  balance: Float!
  type: AccountType!
  institution: Institution
}

enum AccountType {
  CURRENT_ACCOUNT
  CREDIT_CARD
  SAVINGS
  INVESTMENT
  MORTGAGE
  LOAN
  PENSION
  UNKNOWN
}

type Query {
  accounts: [Account!]!
  account(id: Int!): Account!
  institutions: [Institution!]!
  institution(id: ID!): Institution!
}

type Mutation {
  createInstitution(input: CreateInstitutionRequest!): Institution!
  updateInstitution(id: String!, input: UpdateInstitutionRequest!): Institution!
  deleteInstitution(id: String!): Institution!
}

input CreateInstitutionRequest {
  """The name of the institution"""
  name: String!

  """The image path for the institution"""
  image_src: String
}

input UpdateInstitutionRequest {
  """The name of the institution"""
  name: String

  """The image path for the institution"""
  image_src: String
}