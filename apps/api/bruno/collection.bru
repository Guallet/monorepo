meta {
  name: Guallet API
}

auth {
  mode: none
}

docs {
  Requests authenticate with the Better Auth session cookie.
  Run the requests in the Auth folder in order (send OTP, then sign in)
  and Bruno's cookie jar will carry the session for every other request.
}
