[phases.setup]
nixPkgs = ["nodejs_24", "pnpm"]

[phases.install]
cmds = ["pnpm install --frozen-lockfile"]

[phases.build]
cmds = ["pnpm --filter webapp build"]

[variables]
NIXPACKS_STATIC_DIRECTORY = "apps/webapp/dist"
